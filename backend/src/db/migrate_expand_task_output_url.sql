-- =============================================================================
-- 数据迁移脚本：扩展 tasks.output_url 容量
-- 目标：允许持久化 provider 返回的长时效下载 URL，避免任务在成功落库阶段卡住
-- =============================================================================

ALTER TABLE tasks
  MODIFY COLUMN output_url TEXT COMMENT 'Provider 输出 GLB URL';
